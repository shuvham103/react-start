import React from 'react';
import {View, Text, StyleSheet, Button} from 'react-native';

const ColoScreen = () =>{
    return (
        <View>
            <Button title="Add Color"></Button>

        </View>
    );
}

const styles = StyleSheet.create({});

export default ColoScreen;